import React from 'react';
import PropTypes from 'prop-types';
import { connect }from 'react-redux';

function EquationForm({dispatch}){
    return(
        <div>

        </div>

    );
}

EquationForm.propTypes = {
    dispatch: PropTypes.func
}
export default connect()(EquationForm);